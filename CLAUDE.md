请用中文解释，但代码中的函数名保持英文风格，注释请写中文。

## AgentTeam Kit（标准源）

本仓库是 **AgentTeam kit**，不是业务项目。新项目接入见 `docs/agent-team/bootstrap-new-project.md`。

| 入口 | 路径 |
|------|------|
| 边界冻结 | `BOUNDARY.md` |
| kit 说明 | `docs/agent-team/README.md` |
| 接入新项目 | `docs/agent-team/bootstrap-new-project.md` |
| 工作区版本钉 | `docs/agent-team/workspace-version-pin.md` |
| 逐步 SOP | `docs/agent-team/coach-playbook.md` |
| 教练 | `@agent-team-coach` |
| 规范维护 | `@agent-team-maintainer` |

spec **禁止**写入本仓（kit）。写到 code_root / workspace_docs 的 `docs/features/`。

L2（包名、端口、架构）以 code_root 的 `project-architecture.mdc` 与 `test-env.override.md` 为准。
